import 'package:flutter/material.dart';

class KVariables extends StatelessWidget {
  static int sidebarWidth = 200;
  const KVariables({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
